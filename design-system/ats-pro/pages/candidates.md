# Candidate directory override

- Search, filters, export, selection, and bulk actions form one responsive toolbar.
- Preserve a real table on wide screens; use a readable stacked card representation on small screens.
- Bulk actions appear only when selection is non-empty and remain keyboard reachable.
- Name is the primary cell; email/job/date are secondary, status is a labelled badge.
